name: Next Deployment

on:
  push:
    branches: [master]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy using ssh
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          password: ${{ secrets.PASSWORD }}
          port: 22
          script: |
            echo "🚀 Deploying FPTedu-tic-tac-toe..."
            # Đi tới thư mục dự án
            cd server-tictactoe || { echo "❌ Directory server-tictactoe not found"; exit 1; }
            # Bắt buộc nhận code mới
            git fetch origin master || { echo "❌ Failed to fetch code"; exit 1; }
            git reset --hard origin/master || { echo "❌ Failed to reset code"; exit 1; }
            # Sao chép .env.example thành .env nếu cần
            if [ ! -f .env ] && [ -f .env.example ]; then
              cp .env.example .env
              echo "✅ Copied .env.example to .env"
            elif [ ! -f .env.example ]; then
              echo "❌ .env.example does not exist"
              exit 1
            else
              echo "✅ .env already exists, skipping copy"
            fi
            # Ghi các biến môi trường từ GitHub Secrets vào .env
            echo "PORT=3000" >> .env
            echo "ACCESS_TOKEN_SECRET=${{ secrets.ACCESS_TOKEN_SECRET }}" >> .env
            echo "REFRESH_TOKEN_SECRET=${{ secrets.REFRESH_TOKEN_SECRET }}" >> .env
            echo "REFRESH_TOKEN_EXPIRES_IN=7d" >> .env
            # Xóa node_modules và cài lại
            rm -rf node_modules package-lock.json
            npm install
            # Build ứng** Build ứng dụng
            npm run build || { echo "❌ Build failed"; exit 1; }
            # Khởi động lại ứng dụng
            pm2 restart website
            echo "✅ Deployment completed"
